
import { TodoModel } from '../../components/todo/model/todoModel';
import { AppDispatch } from '../index'

export const initialToDo = () => {
  return async (dispatch: AppDispatch) => {

  };
};


export const addToDo = (newTodo: TodoModel) => {
  return async (dispatch: AppDispatch) => {

  };
};
